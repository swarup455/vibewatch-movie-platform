import { Request, Response } from "express";
import { asyncHandler } from "../../utils/asyncHandler.js";

export const continueWithGoogle = asyncHandler(async(req: Request, res: Response) => {
    
})